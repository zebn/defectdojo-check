# Workspace instructions

- This is a dependency-free vanilla HTML, CSS, and JavaScript app.
- Keep the app runnable by opening `index.html` directly in a browser.
- Avoid Docker, backend services, and external runtime dependencies.
- Preserve accessible labels, responsive layout, and local-only test data.
